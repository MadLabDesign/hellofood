'use strict';

describe('myApp.intro module', function() {

  beforeEach(module('myApp.intro'));

  describe('intro controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var introCtrl = $controller('IntroCtrl');
      expect(introCtrl).toBeDefined();
    }));

  });
});